namespace Tetra.Shared;

public static class Constants
{
    public static readonly string RootPath    = Path.GetFullPath(".");
    public static readonly string WwwRootPath = Path.Combine(RootPath, "wwwroot");
}
